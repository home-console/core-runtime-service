# Core Runtime имеет минимальные зависимости
# Используется только стандартная библиотека Python 3.13+

# Опционально для разработки:
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# pytest-html

# Test dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-html
pytest-reporter-html1
pytest-xdist>=3.5.0

# HTTP adapter dependencies (для плагина api_gateway)
fastapi>=0.95.0
starlette>=1.0.1
uvicorn[standard]>=0.22.0
aiohttp>=3.14.0
httpx>=0.27.0
idna>=3.15

# PostgreSQL adapter (опционально)
asyncpg>=0.28.0

# Password hashing
bcrypt>=4.0.0

# SecretStore / credentials encryption (Argon2id KDF)
argon2-cffi>=21.2.0

# JWT tokens
PyJWT>=2.13.0
cryptography>=41.0.0
prometheus-client>=0.20.0

# Network scanner (для network_scanner plugin)
netifaces>=0.11.0

# SSH connect (подключение к хосту по креду из БД)
paramiko>=3.4.0

# WebSocket dependencies (FastAPI + uvicorn WebSocket support)
websockets>=12.0
wsproto>=1.2.0

# Pydantic для валидации моделей
pydantic>=2.0.0
pydantic_settings>=2.0.0

# Linting
ruff>=0.4.0
