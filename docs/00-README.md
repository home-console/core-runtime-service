# Core Runtime Service

> **Минимальное координирующее ядро для plugin-first платформы умного дома**

---

## Что это?

Core Runtime — это **инфраструктурный kernel**, а не backend-приложение.

Он предоставляет минимальный набор примитивов для плагинов:
- **EventBus** — асинхронная доставка событий (pub/sub)
- **ServiceRegistry** — вызов сервисов между плагинами (RPC)
- **Storage** — персистентное key-value хранилище
- **StateEngine** — read-only кеш состояния runtime
- **PluginManager** — управление lifecycle плагинов

**Core намеренно «глупый»:** он не знает доменов (devices, automation, presence), интеграций (Yandex, OAuth) или UI. Вся бизнес-логика реализуется плагинами и модулями.

---

## Для кого эта документация?

### Я новичок в проекте
→ Начните с [03-QUICKSTART.md](03-QUICKSTART.md) — запустите систему за 5 минут

### Я разрабатываю плагины
→ Читайте [02-MODULES-AND-PLUGINS.md](02-MODULES-AND-PLUGINS.md) (реестр контрактов в разработке)

### Я архитектор / maintainer
→ [01-ARCHITECTURE.md](01-ARCHITECTURE.md) — источник истины для архитектурных решений (обновлён 2026-01-09)

### Мне нужно интегрировать vendor API
→ [integrations/yandex/README.md](integrations/yandex/README.md) — пример интеграции (Yandex)

---

## Навигация по документации

### Концепции и архитектура
| Документ | Назначение | Аудитория |
|----------|------------|-----------|
| [01-ARCHITECTURE.md](01-ARCHITECTURE.md) | Инварианты, контракты, boundaries | Maintainers, архитекторы |
| [02-MODULES-AND-PLUGINS.md](02-MODULES-AND-PLUGINS.md) | Когда module vs plugin, migration guide | Разработчики плагинов |
| [04-CORE-RUNTIME-CONTRACT.md](04-CORE-RUNTIME-CONTRACT.md) | Гарантии Core, semantics, limitations | Разработчики Core и плагинов |
| [07-RUNTIME-MODULE-CONTRACT.md](07-RUNTIME-MODULE-CONTRACT.md) | Строгий контракт RuntimeModule и ModuleManager | Разработчики модулей |

### Практическое использование
| Документ | Назначение | Аудитория |
|----------|------------|-----------|
| [03-QUICKSTART.md](03-QUICKSTART.md) | Запуск за 5 минут, первый плагин | Новички |
| [05-REMOTE-PLUGIN-CONTRACT.md](05-REMOTE-PLUGIN-CONTRACT.md) | Контракт для remote plugins (HTTP) | Разработчики remote plugins |
| [06-CONTRACTS.md](06-CONTRACTS.md) | Реестр сервисов и событий | Разработчики плагинов (в разработке) |
| [07-RUNTIME-MODULE-CONTRACT.md](07-RUNTIME-MODULE-CONTRACT.md) | Строгий контракт RuntimeModule и ModuleManager | Разработчики модулей |
| [08-PLUGIN-CONTRACT.md](08-PLUGIN-CONTRACT.md) | Контракт Plugin и Manifest | Разработчики плагинов |
| [09-OPERATIONS.md](09-OPERATIONS.md) | Запуск, environment, deployment | DevOps, SRE (в разработке) |
| [10-TESTING.md](10-TESTING.md) | Как запускать тесты, smoke scripts | QA, разработчики (в разработке) |

### Интеграции
| Документ | Назначение |
|----------|------------|
| [integrations/yandex/README.md](integrations/yandex/README.md) | Интеграция с Яндекс Умным Домом (пример) |

### Проектная информация
| Документ | Назначение |
|----------|------------|
| [99-TODO.md](99-TODO.md) | Roadmap, планы развития |

---

## Философия проекта

### Принципы Core Runtime

1. **Минимализм:** Если функциональность можно вынести в плагин — она НЕ должна быть в Core.
2. **Стабильность:** Core API меняется редко и только с явной причиной.
3. **Предсказуемость:** Простое поведение важнее магических оптимизаций.
4. **Изоляция:** Плагины взаимодействуют только через runtime API, никогда напрямую.

### Non-goals

Core Runtime **не является:**
- ❌ Backend-приложением (нет REST API из коробки)
- ❌ CRUD-системой (нет ORM, нет схем базы данных)
- ❌ Message broker (нет гарантий доставки, нет персистентности событий)
- ❌ Smart home контроллером (не знает devices, scenes, automation)

Если вам нужны эти вещи — они реализуются плагинами или модулями поверх Core.

---

## Текущий статус проекта

**Версия:** 0.2.0 (alpha)  
**Статус:** Активная разработка, архитектура стабилизируется

### Что работает сейчас (stable)
- ✅ EventBus (pub/sub с error isolation)
- ✅ ServiceRegistry (async RPC)
- ✅ Storage API (key-value, SQLite adapter)
- ✅ StateEngine (read-only cache)
- ✅ PluginManager (load/start/stop/unload lifecycle)
- ✅ Built-in modules (devices domain)

### Что в разработке (transitional)
- 🚧 HttpRegistry (декларативные HTTP контракты)
- 🚧 Remote plugins (HTTP-based plugin proxy)
- 🚧 Integrations (Yandex Smart Home, OAuth)

### Что запланировано (experimental)
- 📋 Admin UI (React + декларативные манифесты)
- 📋 Python SDK для разработки плагинов
- 📋 CLI для управления runtime
- 📋 Metrics и observability

---

## Быстрый старт (3 команды)

```bash
# 1. Создать виртуальное окружение и установить зависимости
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# 2. Запустить runtime
python3 main.py

# 3. В другом терминале: посмотреть доступные сервисы
python3 -c "import asyncio; from core.runtime import CoreRuntime; from adapters.sqlite_adapter import SQLiteAdapter; asyncio.run((lambda: print('Runtime ready'))())"
```

Подробнее: [03-QUICKSTART.md](03-QUICKSTART.md)

---

## Вопросы и ответы

### В чём разница между module и plugin?
**Module** — обязательная доменная логика, регистрируется в CoreRuntime при старте.  
**Plugin** — опциональное расширение, загружается через PluginManager.

Подробно: [02-MODULES-AND-PLUGINS.md](02-MODULES-AND-PLUGINS.md)

### Почему Core не использует FastAPI/Flask?
Core — это kernel, а не web-сервер. HTTP endpoints реализуются плагином `api_gateway`, который читает декларативные контракты из `HttpRegistry` и проецирует их на FastAPI.

### Можно ли использовать Core без плагинов?
Технически да, но бессмысленно. Core без плагинов — это пустая инфраструктура. Минимально полезная система = Core + built-in modules (devices).

### Как добавить новую интеграцию?
Создайте integration plugin, который:
1. Получает данные из vendor API
2. Публикует `external.*` события
3. Подписывается на `internal.*` команды
4. Не хранит состояние напрямую (используйте Storage)

Пример: [integrations/yandex/README.md](integrations/yandex/README.md)

---

## Лицензия

MIT License — используйте свободно, но без гарантий.

---

## Контрибьюция

Проект находится в стадии активной разработки. Перед созданием PR:
1. Прочитайте [01-ARCHITECTURE.md](01-ARCHITECTURE.md) — инварианты системы
2. Убедитесь, что тесты проходят: `pytest -q`
3. Проверьте, что изменения не ломают архитектурные инварианты

---

**Следующий шаг:** [03-QUICKSTART.md](03-QUICKSTART.md) — запустите систему за 5 минут
