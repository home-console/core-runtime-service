# Архитектурный анализ авторизации

**Дата анализа:** 2024  
**Вердикт:** Базовая авторизация реализована ✅ | Архитектура готова к расширенной авторизации ✅

---

## Краткий вердикт

1. **Базовая авторизация (Policy Layer): РЕАЛИЗОВАНА ✅**
   - Единая точка принятия решений: `modules/api/authz.py`
   - Явная политика: `ACTION_SCOPE_MAP`
   - Resource-Based Authorization с ACL (ownership + shared_with)

2. **Готовность к расширенной авторизации: ГОТОВА ✅**
   - Архитектура позволяет добавлять новые правила без рефакторинга
   - Слои разделены (Authentication / Authorization)
   - Проверки изолированы на boundary layer

---

## Детальный анализ по критериям

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| **1. Единая точка принятия решения** | ✅ Есть | `modules/api/authz.py` содержит функции `check()` и `require()`, которые являются единственным местом проверки доступа. Все проверки проходят через эти функции. |
| **2. Отделение Authentication от Authorization** | ✅ Разделены | **Authentication** (`modules/api/auth/middleware.py`): только валидирует токены/ключи/сессии, создаёт `RequestContext`. **Authorization** (`modules/api/authz.py`): работает только с `RequestContext`, не знает про transport, cookies, headers. |
| **3. Проверки доступа в доменной логике** | ⚠️ Только на API уровне | Проверки выполняются ТОЛЬКО в `modules/api/module.py` (API handlers, строки 172, 197). Сервисы (`ServiceRegistry.call()`) НЕ проверяют доступ сами. Core и доменные модули НЕ знают про auth. **Проблема:** Если сервис вызывается напрямую (не через HTTP), проверка не выполняется. |
| **4. Явность политики** | ✅ Явная | `ACTION_SCOPE_MAP` в `modules/api/authz.py` (строки 30-104) явно определяет mapping: `action → required scope`. Можно в одном месте увидеть все требования. |
| **5. Расширяемость без рефакторинга** | ✅ Готова | Для добавления правила "пользователь может менять ТОЛЬКО свои устройства": 1) Добавить проверку в `authz.check()` (строки 209-237) - уже есть поддержка `owner_id`; 2) Обновить `ACTION_SCOPE_MAP` если нужно; 3) Передавать `resource` из handler. **Не требуется:** менять сигнатуры сервисов, переписывать auth, лезть в Core. |
| **6. Запрещённые запахи** | ⚠️ Есть проблемы | См. раздел "Конкретные проблемы" ниже |

---

## Конкретные проблемы

### 1. Проверки доступа только на HTTP уровне

**Файл:** `modules/api/module.py`  
**Строки:** 172, 197  
**Проблема:** Проверки выполняются только в HTTP handlers. Если сервис вызывается напрямую через `service_registry.call()` (например, из другого модуля или плагина), проверка не выполняется.

**Пример:**
```python
# Это защищено:
GET /api/devices/get?id=123  # Проверка в handler

# Это НЕ защищено:
await runtime.service_registry.call("devices.get", "123")  # Прямой вызов
```

**Влияние:** Сервисы "доверяют API", но не защищены от прямых вызовов.

---

### 2. Проверка context в admin.auth.me

**Файл:** `modules/admin/module.py`  
**Строки:** 751-752  
**Проблема:** Сервис `admin.auth.me` проверяет `context` напрямую, а не через `authz.require()`:

```python
if context is None or context.user_id is None:
    return {"ok": False, "error": "not authenticated"}
```

**Влияние:** Небольшое нарушение единой точки принятия решений. Лучше использовать `authz.require()`.

---

### 3. Устаревшая функция check_service_scope

**Файл:** `modules/api/auth/utils.py`  
**Строки:** 37-82  
**Проблема:** Функция `check_service_scope()` помечена как DEPRECATED, но всё ещё существует. Может использоваться в старом коде.

**Влияние:** Потенциальная путаница, но не критично.

---

### 4. Специальные случаи в authz.check()

**Файл:** `modules/api/authz.py`  
**Строки:** 141-151  
**Проблема:** Хардкод специальных случаев (OAuth endpoints, login, первый API key) внутри функции проверки.

**Влияние:** Усложняет логику, но не блокирует расширение.

---

## Что можно делать ДАЛЬШЕ

✅ **Можно добавлять:**
- Новые правила в `ACTION_SCOPE_MAP`
- Новые проверки ресурсов в `authz.check()` (ownership, shared_with уже есть)
- Новые типы ресурсов (tenant, ACL группы)
- Расширенные политики без изменения сигнатур сервисов

✅ **Архитектура готова к:**
- Добавлению ownership проверок для любых ресурсов
- Добавлению tenant-based авторизации
- Добавлению ролевых моделей (через расширение `RequestContext`)
- Добавлению временных разрешений

---

## Что делать РАНО

❌ **Не готово к:**
- Прямым вызовам сервисов без проверки (нужно добавить проверку в `ServiceRegistry.call()` или обернуть сервисы)
- Распределённым проверкам (если сервисы вызываются из разных процессов)

❌ **Требует рефакторинга:**
- Защита сервисов от прямых вызовов (если это требование)
- Унификация проверок (перенести проверку из `admin.auth.me` в `authz.require()`)

---

## Рекомендации

### Немедленные улучшения (опционально):

1. **Перенести проверку из `admin.auth.me` в `authz.require()`:**
   ```python
   # Вместо:
   if context is None or context.user_id is None:
       return {"ok": False, "error": "not authenticated"}
   
   # Использовать:
   authz_require(context, "admin.auth.me")
   ```

2. **Удалить устаревшую функцию `check_service_scope()`** или убедиться, что она не используется.

### Будущие улучшения (если потребуется):

1. **Добавить проверку в `ServiceRegistry.call()`** (опционально, если требуется защита от прямых вызовов):
   ```python
   async def call(self, service_name: str, context: Optional[RequestContext] = None, **kwargs):
       if context:
           authz_require(context, service_name)
       # ... вызов сервиса
   ```
   **НО:** Это потребует передачи `RequestContext` во все вызовы сервисов, что может быть избыточно.

2. **Вынести специальные случаи из `authz.check()`** в конфигурацию или отдельную функцию.

---

## Выводы

### ✅ Сильные стороны:

1. **Единая точка принятия решений** — все проверки через `authz.check()` / `authz.require()`
2. **Явная политика** — `ACTION_SCOPE_MAP` показывает все требования
3. **Разделение слоёв** — Authentication и Authorization разделены
4. **Resource-Based Authorization** — поддержка ownership и shared_with уже есть
5. **Расширяемость** — можно добавлять новые правила без рефакторинга

### ⚠️ Слабые стороны:

1. **Защита только на HTTP уровне** — прямые вызовы сервисов не защищены
2. **Небольшие нарушения единой точки** — проверка в `admin.auth.me`

### 🎯 Итог:

**Базовая авторизация реализована и работает корректно.**  
**Архитектура готова к добавлению расширенной авторизации (ownership / tenant / ACL) без рефакторинга.**

Для добавления правила "пользователь может менять ТОЛЬКО свои устройства" достаточно:
1. Убедиться, что device имеет `owner_id`
2. Передавать `resource={"owner_id": device["owner_id"]}` в `authz_require()`
3. Логика уже реализована в `authz.check()` (строки 213-216)
