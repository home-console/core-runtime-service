# Структура файлов проекта

## 🗂 Основные файлы

| Файл | Строк | Назначение |
|------|-------|------------|
| `main.py` | ~70 | Точка входа в Runtime, запуск и graceful shutdown |
| `demo.py` | ~115 | Демонстрация всех возможностей Core Runtime |
| `config.py` | ~30 | Конфигурация Runtime (db_path, timeouts) |
| `.gitignore` | ~30 | Исключения для Git (БД, кэш, временные файлы) |
| `requirements.txt` | ~5 | Зависимости проекта (пусто — только stdlib) |

---

## 🎯 Ядро Runtime (core/)

| Файл | Строк | Назначение |
|------|-------|------------|
| `__init__.py` | ~20 | Экспорт публичных классов ядра |
| `runtime.py` | ~85 | **CoreRuntime** — главный класс, координатор всех компонентов |
| `event_bus.py` | ~120 | **EventBus** — асинхронная шина событий (pub/sub) |
| `service_registry.py` | ~105 | **ServiceRegistry** — реестр сервисов (RPC) |
| `state_engine.py` | ~95 | **StateEngine** — in-memory хранилище состояния |
| `storage.py` | ~70 | **Storage** — API для работы с персистентным хранилищем |
| `plugin_manager.py` | ~195 | **PluginManager** — управление lifecycle плагинов |

**Итого ядро:** ~690 строк

---

## 🔌 Адаптеры (adapters/)

| Файл | Строк | Назначение |
|------|-------|------------|
| `__init__.py` | ~10 | Экспорт адаптеров |
| `storage_adapter.py` | ~80 | **StorageAdapter** — абстрактный интерфейс для storage |
| `sqlite_adapter.py` | ~105 | **SQLiteAdapter** — SQLite реализация без ORM |

**Итого адаптеры:** ~195 строк

---

## 🧩 Плагины (plugins/)

| Файл | Строк | Назначение |
|------|-------|------------|
| `__init__.py` | ~10 | Экспорт базовых классов плагинов |
| `base_plugin.py` | ~115 | **BasePlugin** — базовый класс для всех плагинов |
| `example_plugin.py` | ~100 | **ExamplePlugin** — пример реализации плагина |

**Итого плагины:** ~225 строк

---

## 📚 Документация

| Файл | Строк | Размер | Назначение |
|------|-------|--------|------------|
| `README.md` | ~420 | 14 KB | Основная документация проекта |
| `ARCHITECTURE.md` | ~630 | 21 KB | Архитектурные решения и паттерны |
| `03-QUICKSTART.md` | ~320 | 10 KB | Быстрый старт и примеры использования |
| `TODO.md` | ~280 | 9 KB | Roadmap и планы развития |
| `OVERVIEW.md` | ~310 | 10 KB | Общий обзор проекта |
| `docs.md` | ? | ? | (Существующий файл, игнорируется Git) |

**Итого документация:** ~1960 строк, ~64 KB

---

## 📊 Статистика

### Код
- **Всего строк кода (Python):** 1315
- **Компонентов ядра:** 7
- **Адаптеров:** 2
- **Примеров плагинов:** 1
- **Зависимостей:** 0 (только stdlib)

### Документация
- **Файлов документации:** 5
- **Строк документации:** ~1960
- **Размер документации:** ~64 KB

### Соотношение
- **Код:Документация** ≈ 1:1.5 (отличное соотношение!)
- **Комментариев в коде:** ~25% (все на русском)

---

## 🎨 Организация кода

### Принципы
1. **Один файл = один компонент** (Single Responsibility)
2. **Плоская структура** (максимум 2 уровня вложенности)
3. **Явные имена** (русские комментарии, английские имена)
4. **Минимализм** (каждая строка кода оправдана)

### Соглашения
- Все классы — CamelCase
- Все функции и переменные — snake_case
- Все комментарии и docstrings — на русском языке
- Все имена переменных/классов — на английском

### Структура файла
```python
"""
Модуль описание.

Детальное описание назначения модуля.
"""

# Импорты stdlib
import asyncio
from typing import Any

# Импорты проекта
from core.something import Something

# Константы
CONSTANT = "value"

# Классы
class MyClass:
    """Описание класса."""
    
    def method(self):
        """Описание метода."""
        pass
```

---

## 🔍 Детали компонентов

### CoreRuntime (runtime.py)
**Задача:** Координация всех компонентов.

**Зависимости:**
- EventBus
- ServiceRegistry
- StateEngine
- Storage
- PluginManager

**Публичные методы:**
- `start()` — запустить Runtime
- `stop()` — остановить Runtime
- `shutdown()` — полное завершение

---

### EventBus (event_bus.py)
**Задача:** Pub/Sub обмен событиями.

**Структура данных:**
```python
_handlers: dict[str, list[EventHandler]]
# event_type -> [handler1, handler2, ...]
```

**Публичные методы:**
- `subscribe(event_type, handler)`
- `unsubscribe(event_type, handler)`
- `publish(event_type, data)`

---

### ServiceRegistry (service_registry.py)
**Задача:** RPC вызовы между плагинами.

**Структура данных:**
```python
_services: dict[str, ServiceFunc]
# service_name -> function
```

**Публичные методы:**
- `register(service_name, func)`
- `unregister(service_name)`
- `call(service_name, *args, **kwargs)`

---

### StateEngine (state_engine.py)
**Задача:** In-memory состояние Runtime.

**Структура данных:**
```python
_state: dict[str, Any]
# key -> value (любой тип)
```

**Публичные методы:**
- `get(key)`
- `set(key, value)`
- `delete(key)`
- `exists(key)`

---

### Storage (storage.py)
**Задача:** Персистентное хранилище.

**Модель:** `namespace + key + JSON value`

**Публичные методы:**
- `get(namespace, key)`
- `set(namespace, key, value)`
- `delete(namespace, key)`
- `list_keys(namespace)`

---

### PluginManager (plugin_manager.py)
**Задача:** Lifecycle плагинов.

**Состояния:**
- UNLOADED
- LOADED
- STARTED
- STOPPED
- ERROR

**Публичные методы:**
- `load_plugin(plugin)`
- `start_plugin(plugin_name)`
- `stop_plugin(plugin_name)`
- `unload_plugin(plugin_name)`

---

### BasePlugin (base_plugin.py)
**Задача:** Базовый класс для плагинов.

**Lifecycle методы:**
1. `on_load()` — регистрация сервисов, подписка на события
2. `on_start()` — запуск фоновых задач
3. `on_stop()` — остановка задач
4. `on_unload()` — очистка ресурсов

**Обязательный атрибут:**
- `metadata: PluginMetadata` — метаданные плагина

---

## 🎯 Точки расширения

### Добавить новый адаптер
1. Наследоваться от `StorageAdapter`
2. Реализовать все абстрактные методы
3. Использовать при создании Runtime

### Добавить новый компонент
1. Создать файл в `core/`
2. Реализовать класс
3. Добавить в `CoreRuntime.__init__`
4. Экспортировать в `core/__init__.py`

### Создать плагин
1. Наследоваться от `BasePlugin`
2. Реализовать `metadata` property
3. Переопределить lifecycle методы
4. Зарегистрировать в Runtime

---

## 📋 Чеклист для нового файла

При создании нового файла:
- [ ] Добавить module docstring на русском
- [ ] Добавить в соответствующий `__init__.py`
- [ ] Добавить docstrings ко всем классам/функциям
- [ ] Добавить type hints
- [ ] Соблюсти naming conventions
- [ ] Обновить эту документацию

---

## 🔗 Зависимости между файлами

```
main.py
├── config.py
└── core/runtime.py
    ├── core/event_bus.py
    ├── core/service_registry.py
    ├── core/state_engine.py
    ├── core/storage.py
    │   └── adapters/storage_adapter.py
    │       └── adapters/sqlite_adapter.py
    └── core/plugin_manager.py
        └── plugins/base_plugin.py
            └── plugins/example_plugin.py
```

**Принцип:** Нет циклических зависимостей, только однонаправленные.

---

Последнее обновление: 2026-01-06
