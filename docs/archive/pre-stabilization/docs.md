📦 Требования к функциональности

1) Менеджер жизненного цикла плагинов

- обнаружение плагинов через entry points (точки входа)
- валидация манифеста плагина
- проверка зависимостей между плагинами
- вызов этапов lifecycle: `setup` / `start` / `stop` / `unload`
- поддержка горячей перезагрузки (hot-reload)

2) Шина событий (EventBus, ядро)

- асинхронная публикация/подписка (pub/sub)
- поддержка пространств имён для событий
- не хранит бизнес-логику — только транспорт событий
- схемы и валидация полезной нагрузки событий

3) Реестр сервисов (Service Registry)

- регистрация: `register(name, handler, schema)`
- вызов сервиса: `call(name, params)` → валидация → маршрутизация
- поддержка проксирования на удалённые инстансы

4) Движок состояния (State Engine)

- API: `set(namespace, key, value)` / `get(namespace, key)` / `delete`
- генерация событий при изменениях состояния

5) API хранения (персистентное)

- абстракция над БД (плагин-адаптеры для конкретных хранилищ)
- неймспейсы/пространства имён для данных плагинов
- простой CRUD-интерфейс
- система миграций схем данных

6) Адаптеры

- адаптер FastAPI (только транспорт, без бизнес-логики)
- CLI-адаптер
- MQTT-адаптер
- возможность добавлять gRPC, WebSocket и другие адаптеры

7) Безопасность

- семантика изоляции плагинов (ограничение доступа к ресурсам)
- аутентификация для удалённых вызовов
- подпись и проверка целостности IPC/межкомпонентных вызовов
- разграничение прав на события и вызовы сервисов

🛠 Технические ограничения

- Python 3.12+
- все внутренние API — асинхронные (`async` / `await`)
- в ядре не должно быть бизнес-логики
- отсутствие прямых моделей БД в ядре; всё через контракты
- взаимодействие только по контрактам/интерфейсам

📁 Структура директорий
```
core/
 ├─ kernel.py
 ├─ plugin_manager.py
 ├─ event_bus.py
 ├─ service_registry.py
 ├─ state_engine.py
 ├─ storage_adapter.py
 ├─ ipc/
 │    ├─ http_proxy.py
 │    └─ grpc_proxy.py
 ├─ errors.py
 └─ contracts/
      ├─ event.py
      ├─ service.py
      ├─ plugin.py
      └─ state.py
```

📌 Пример контракта
```python
from typing import Protocol

class PluginContract(Protocol):
    name: str
    version: str
    requires: list[str]

    async def setup(self, core): ...
    async def start(self): ...
    async def stop(self): ...
    async def unload(self): ...
```

🧪 Инфраструктура

- адаптеры БД (PostgreSQL / SQLite)
- Redis — опционально, для кэширования
- метрики для Prometheus

🧰 Тестирование

- модульные тесты для контрактов
- интеграционный тестовый хаб (mock SDK)
- сценарные (end-to-end) тесты

🧩 Документация

- справочник API
- схема манифеста плагина
- руководство по разработке плагинов
- примеры использования SDK

🚀 Доставка и поддержка

- версионированные релизы
- политика обратной совместимости
- семантическое версионирование

Если нужно, могу дополнительно:
- привести пример манифеста плагина на русском,
- сгенерировать шаблон плагина по этим контрактам.