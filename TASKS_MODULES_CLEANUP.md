# 📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ
## Ликвидация папки `modules\` (дублер модулей)

**Дата:** 26 марта 2026 г.  
**Приоритет:** Высокий  
**Статус:** В выполнении

---

## 1. АНАЛИЗ ТЕКУЩЕЙ СИТУАЦИИ

### 1.1. Обнаруженные факты

В проекте существуют **две папки с модулями**:

| Папка | Размер | Файлов | Строк кода |
|-------|--------|--------|------------|
| `modules/` | 2,3 МБ | 227 файлов | ~11,000 строк |
| `modules\/` | 380 КБ | 43 файла | ~2,600 строк |

### 1.2. Структура папки `modules\`

```
modules\
├── execution/          (6 файлов + 2 подпапки)
│   ├── __init__.py
│   ├── backend.py
│   ├── controller.py
│   ├── policy.py
│   ├── scheduler.py
│   ├── trace.py
│   ├── backends/      (3 файла)
│   └── runner/        (3 файла)
│
└── security/          (15 файлов + 2 подпапки)
    ├── __init__.py
    ├── crypto.py
    ├── policy_engine.py
    ├── rate_limiter.py
    ├── rbac_models.py
    ├── secret_policy.py
    ├── secret_store.py
    ├── secret_store_adapter.py
    ├── secure_memory.py
    ├── tpm.py
    ├── vault_hardening.py
    ├── vault_session.py
    ├── mfa/           (6 файлов)
    └── risk/          (5 файлов)
    └── trust/         (7 файлов)
```

### 1.3. Результаты сравнения файлов

**Критично:** Файлы в `modules\` являются **ПОЛНЫМИ ДУБЛИКАТАМИ** файлов из `modules/`:

| Файл | Статус |
|------|--------|
| `modules\/execution/controller.py` | ✅ 100% дубль |
| `modules\/execution/scheduler.py` | ✅ 100% дубль |
| `modules\/execution/backend.py` | ✅ 100% дубль |
| `modules\/execution/policy.py` | ✅ 100% дубль |
| `modules\/execution/trace.py` | ✅ 100% дубль |
| `modules\/security/crypto.py` | ✅ 100% дубль |
| `modules\/security/__init__.py` | ✅ 100% дубль |
| Все подпапки `mfa/`, `risk/`, `trust/` | ✅ 100% дубли |

### 1.4. Ключевые отличия

| Аспект | `modules/execution/` | `modules\/execution/` |
|--------|---------------------|----------------------|
| `module.py` | ✅ Есть (578 строк) | ❌ **ОТСУТСТВУЕТ** |
| `remote_executor.py` | ✅ Есть (211 строк) | ❌ **ОТСУТСТВУЕТ** |
| `router.py` | ✅ Есть (184 строки) | ❌ **ОТСУТСТВУЕТ** |
| `service_executor.py` | ✅ Есть (116 строк) | ❌ **ОТСУТСТВУЕТ** |

| Аспект | `modules/security/` | `modules\/security/` |
|--------|---------------------|----------------------|
| `auth_service.py` | ✅ Есть | ❌ **ОТСУТСТВУЕТ** |

### 1.5. Используется ли `modules\` в коде?

**Результат поиска импортов:**
```bash
# Поиск импортов из modules\
from modules\\\.|import modules\\.
```
**Результат:** ❌ **0 совпадений** — папка `modules\` НЕ ИСПОЛЬЗУЕТСЯ в коде.

---

## 2. ПРОБЛЕМЫ

### 2.1. Основные проблемы

1. **Дублирование кода** — 43 файла (2,600 строк) дублируют функционал из `modules/`
2. **Бесполезное占用 места** — 380 КБ дискового пространства
3. **Риск путаницы** — разработчики могут случайно импортировать из неправильного пути
4. **Проблемы с навигацией** — IDE показывают два одинаковых модуля
5. **Неясность архитектуры** — непонятно, какая папка каноническая

### 2.2. Происхождение папки

Судя по структуре имён (`modules\` с обратным слэшем), папка могла быть создана:
- При работе на Windows-машине
- В результате ошибки скрипта миграции
- Как временная копия, которую забыли удалить

---

## 3. ЗАДАЧИ

### 3.1. Фаза 1: Аудит и резервное копирование

**Задача 1.1:** Создать резервную копию папки `modules\`
```bash
# Сохранить в git stash или временную папку за пределами проекта
cp -r modules\ /tmp/modules_backup_$(date +%Y%m%d)
```

**Задача 1.2:** Проверить, нет ли незакоммиченных изменений
```bash
git status modules\
git diff modules\
```

**Критерий завершения:** Резервная копия создана, изменения зафиксированы.

---

### 3.2. Фаза 2: Финальная проверка использования

**Задача 2.1:** Поиск во всех файлах проекта
```bash
# Поиск любых упоминаний modules\ (включая конфиги, документацию)
rg "modules\\\\" --type-add 'config:*.toml' --type-add 'config:*.yaml' --type-add 'config:*.yml'
```

**Задача 2.2:** Проверка в CI/CD конфигурациях
```bash
# Проверить .github/, scripts/, Dockerfile
grep -r "modules\\" .github/ scripts/ Dockerfile 2>/dev/null || true
```

**Критерий завершения:** Подтверждено, что `modules\` нигде не используется.

---

### 3.3. Фаза 3: Удаление папки

**Задача 3.1:** Удалить папку `modules\`
```bash
rm -rf modules\
```

**Задача 3.2:** Закоммитить удаление
```bash
git add -A
git commit -m "refactor: удалить дублирующую папку modules\

- Папка содержала 43 файла-дубля из modules/
- Не использовалась в коде (0 импортов)
- Занимала 380 КБ дискового пространства
- Создавала путаницу при навигации"
```

**Критерий завершения:** Папка удалена, изменения закоммичены.

---

### 3.4. Фаза 4: Валидация

**Задача 4.1:** Запустить тесты
```bash
pytest -q
```

**Задача 4.2:** Проверить сборку
```bash
# Если есть build-скрипт
python -m py_compile main.py
```

**Задача 4.3:** Убедиться, что импорты работают
```bash
python -c "from modules.execution import ExecutionControllerImpl; print('OK')"
python -c "from modules.security import SecretStore; print('OK')"
```

**Критерий завершения:** Все тесты зелёные, импорты работают.

---

## 4. АРХИТЕКТУРНЫЕ ВЫВОДЫ

### 4.1. Состояние модулей `modules/`

Папка `modules/` находится в **хорошем состоянии**:

| Модуль | Статус | Файлов |
|--------|--------|--------|
| `execution/` | ✅ Канонический | 12 файлов + module.py |
| `security/` | ✅ Канонический | 16 файлов + подпапки |
| `admin/` | ✅ Активен | 3 файла |
| `agent/` | ✅ Активен | 4 файла |
| `api/` | ✅ Активен | 10+ файлов |
| `credentials/` | ✅ Активен | 8 файлов |
| `devices/` | ✅ Активен | 6 файлов |
| ... | ... | ... |

### 4.2. Missing модули (отсутствуют в `modules\`)

Следующие модули **ЕСТЬ в `modules/`**, но **ОТСУТСТВУЮТ в `modules\`**:

| Модуль | Назначение |
|--------|------------|
| `admin/` | Админ-панель, SSH-терминал |
| `agent/` | Управление агентами, enrollment |
| `agents/` | Агентская система (дубль agent/) |
| `api/` | HTTP/WebSocket API, auth middleware |
| `auth/` | handlers для авторизации |
| `automation/` | Автоматизация сценариев |
| `capability/` | Реестр capability |
| `credentials/` | Управление секретами, rotation |
| `devices/` | Управление устройствами |
| `event_bus/` | Шина событий |
| `events/` | Валидация событий |
| `hooks/` | Система хуков |
| `idempotency/` | Идемпотентность операций |
| `inspector/` | Инспектор выполнения |
| `integrations/` | Интеграции |
| `logger/` | Логирование |
| `marketplace/` | Marketplace, транзакции |
| `metrics/` | Метрики |
| `monitoring/` | Мониторинг |
| `operations/` | Операции, handlers |
| `operation_source/` | Источники операций |
| `plugins/` | Плагины, isolation |
| `policy/` | Policy engine |
| `presence/` | Presence-система |
| `product_api/` | Product API |
| `request_logger/` | Логирование запросов |
| `retry_policy/` | Политики повторных попыток |
| `routing/` | Роутинг |
| `ssh/` | SSH execution |
| `storage/` | Storage factory, manager |

**Вывод:** Папка `modules\` содержала только ~15% модулей, и все они — дубли.

---

## 5. РИСКИ И МИТИГАЦИЯ

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Потеря уникальных файлов | Низкая | Среднее | Резервное копирование перед удалением |
| Сломанные импорты | Низкая | Высокое | Проверка grep перед удалением |
| Падение тестов | Низкая | Высокое | Запуск pytest после удаления |
| Случайное удаление `modules/` | Низкая | Критичное | Внимательная проверка пути |

---

## 6. ЧЕК-ЛИСТ ВЫПОЛНЕНИЯ

- [ ] 1. Создать резервную копию `modules\`
- [ ] 2. Проверить `git status modules\`
- [ ] 3. Выполнить поиск импортов (`rg "modules\\\\"`)
- [ ] 4. Удалить папку `rm -rf modules\`
- [ ] 5. Закоммитить удаление
- [ ] 6. Запустить `pytest -q`
- [ ] 7. Проверить импорты
- [ ] 8. Обновить документацию (если упоминается)

---

## 7. КОМАНДЫ ДЛЯ ВЫПОЛНЕНИЯ

```bash
# 1. Резервное копирование
cp -r 'modules\' /tmp/modules_backup_$(date +%Y%m%d_%H%M%S)

# 2. Проверка использования
rg "modules\\\\|modules\\\\\\\\" --type py

# 3. Проверка git
git status 'modules\'
git diff 'modules\'

# 4. Удаление
rm -rf 'modules\'

# 5. Коммит
git add -A
git commit -m "refactor: удалить дублирующую папку modules\\

- 43 файла-дубля из modules/
- 0 импортов в коде
- 380 КБ удалено"

# 6. Валидация
pytest -q
python -c "from modules.execution import ExecutionControllerImpl; print('✓')"
python -c "from modules.security import SecretStore; print('✓')"
```

---

## 8. ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения:

✅ **Удалено:**
- 43 дублирующих файла
- 380 КБ дискового пространства
- Источник путаницы для разработчиков

✅ **Осталось:**
- `modules/` — единственный канонический источник модулей (227 файлов, 2,3 МБ)
- Чистая архитектура без дублей
- Все тесты проходят

---

**Исполнитель:** AI Assistant  
**Дедлайн:** 1 рабочий день  
**Статус:** ⏳ В выполнении
